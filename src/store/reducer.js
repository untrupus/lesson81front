import {
    FETCH_LINK_ERROR,
    FETCH_LINK_REQUEST,
    FETCH_LINK_SUCCESS
} from "./actionsTypes";

const initialState = {
    link: ''
};

const reducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
};

export default reducer;